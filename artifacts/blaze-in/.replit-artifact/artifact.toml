kind = "web"
previewPath = "/"
title = "Blaze.in"
version = "1.0.0"
id = "artifacts/blaze-in"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 21758

[services.development]
run = "pnpm --filter @workspace/blaze-in run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/blaze-in", "run", "build" ]
publicDir = "artifacts/blaze-in/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "21758"
BASE_PATH = "/"
